import { SpokenLanguagesPipe } from './spoken-languages.pipe';

describe('SpokenLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SpokenLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
